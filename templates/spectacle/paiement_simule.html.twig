{% extends 'base.html.twig' %}

{% block title %}Confirmation de Réservation{% endblock %}

{% block body %}
{# On garde la même barre de navigation pour la cohérence #}
<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
    <div class="container">
        <a class="navbar-brand" href="{{ path('app_home') }}">🎭 SpectacleApp</a>
        <div class="collapse navbar-collapse">
            <ul class="navbar-nav ms-auto">
                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link text-white" href="{{ path('app_mes_reservations') }}">📋 Mes Réservations</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link btn btn-outline-danger btn-sm ms-2" href="{{ path('app_logout') }}">Déconnexion</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow-sm border-0">
                <div class="card-header bg-success text-white text-center py-3">
                    <h3 class="mb-0">✅ Paiement Réussi (Simulé)</h3>
                </div>
                <div class="card-body p-4">
                    <p class="text-center text-muted mb-4">Merci pour votre achat ! Voici le récapitulatif de votre commande pour le spectacle <strong>{{ spectacle.titre }}</strong>.</p>
                    
                    <ul class="list-group list-group-flush mb-4">
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            Nombre de places
                            <span class="badge bg-primary rounded-pill">{{ nbPersonnes }}</span>
                        </li>
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            Prix unitaire
                            <span>{{ prixUnitaire }} €</span>
                        </li>
                        <li class="list-group-item d-flex justify-content-between align-items-center fw-bold fs-5">
                            Total payé
                            <span class="text-success">{{ prixTotal }} €</span>
                        </li>
                        <li class="list-group-item d-flex justify-content-between align-items-center small text-muted">
                            Email de confirmation envoyé à
                            <span>{{ emailClient }}</span>
                        </li>
                    </ul>

                    <div class="text-center">
                        <a href="{{ path('app_home') }}" class="btn btn-outline-dark w-100">Retour à l'accueil</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}