{% extends 'base.html.twig' %}

{% block title %}Mes Réservations{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1 class="mb-4">📋 Mes Réservations</h1>

    {% if reservations is empty %}
        <div class="alert alert-info">
            Vous n'avez pas encore effectué de réservation. 
            <a href="{{ path('app_home') }}">Voir les spectacles</a>
        </div>
    {% else %}
        <div class="table-responsive">
            <table class="table table-hover shadow-sm">
                <thead class="table-dark">
                    <tr>
                        <th>Spectacle</th>
                        <th>Date de commande</th>
                        <th>Places</th>
                        <th>Prix Total</th>
                    </tr>
                </thead>
                <tbody>
                {% for r in reservations %}
                    <tr>
                        <td class="fw-bold">{{ r.spectacle.titre }}</td>
                        <td>{{ r.dateReservation|date('d/m/Y H:i') }}</td>
                        <td><span class="badge bg-primary">{{ r.quantite }}</span></td>
                        <td>{{ r.prixTotal }} €</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    {% endif %}
    
    <a href="{{ path('app_home') }}" class="btn btn-outline-secondary mt-3">Retour à l'accueil</a>
</div>
{% endblock %}