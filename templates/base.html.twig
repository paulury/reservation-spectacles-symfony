<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}SPECTACLE BIC{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2290%22>🎭</text></svg>">
        
        
        <link rel="stylesheet" href="https://bootswatch.com/5/sketchy/bootstrap.min.css">
        
        
        <link href="https://fonts.googleapis.com/css2?family=Cabin+Sketch:wght@400;700&display=swap" rel="stylesheet">

        <style>
            body {
                font-family: 'Cabin Sketch', cursive;
                background-color: #fdfdfd;
            }
            .navbar-brand {
                font-size: 1.8rem;
                font-weight: bold;
            }
            .card {
                
                border-width: 2px !important;
            }
            h1, h2, h3 {
                text-transform: uppercase;
            }
            
            .btn:hover {
                transform: rotate(-1deg);
            }
        </style>

        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-5 shadow">
            <div class="container">
                <a class="navbar-brand" href="{{ path('app_home') }}">🎭 SPECTACLE BIC</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarColor02">
                    <ul class="navbar-nav me-auto">
                        <li class="nav-item">
                            <a class="nav-link active" href="{{ path('app_home') }}">Accueil</a>
                        </li>
                        {% if app.user %}
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_mes_reservations') }}">Mes Billets</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link text-warning" href="{{ path('admin') }}">Dashboard Admin</a>
                            </li>
                        {% endif %}
                    </ul>
                    <div class="d-flex">
                        {% if app.user %}
                            <span class="navbar-text me-3 text-white">Bonjour, {{ app.user.userIdentifier }}</span>
                            <a href="{{ path('app_logout') }}" class="btn btn-outline-danger">Déconnexion</a>
                        {% else %}
                            <a href="{{ path('app_login') }}" class="btn btn-outline-light me-2">Connexion</a>
                            <a href="{{ path('app_register') }}" class="btn btn-light">S'inscrire</a>
                        {% endif %}
                    </div>
                </div>
            </div>
        </nav>

        <div class="container">
            {# Affichage des messages flash (Succès / Erreur) #}
            {% for label, messages in app.flashes %}
                {% for message in messages %}
                    <div class="alert alert-{{ label }} alert-dismissible fade show" role="alert">
                        {{ message }}
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                {% endfor %}
            {% endfor %}

            {% block body %}{% endblock %}
        </div>

        <footer class="text-center mt-5 py-4 border-top">
            <p>&copy; {{ "now"|date("Y") }} - PROJET MEILLEUR BIC</p>
        </footer>

        
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
        {% block javascripts %}{% endblock %}
    </body>
</html>